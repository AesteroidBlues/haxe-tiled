enum Orientation
{
    Invalid;
    Orthogonal;
    Isometric;
    Staggered;
    Hexagonal;
}

enum RenderOrder
{
    Invalid;
    RightDown;
    RightUp;
    LeftDown;
    LeftUp;
}

enum StaggerAxis
{
    Invalid;
    X;
    Y;
}

enum StaggerIndex
{
    Invalid;
    Even;
    Odd;
}
